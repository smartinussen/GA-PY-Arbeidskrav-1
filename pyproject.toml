[project]
name = "py-arbeidskrav-1"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.6",
    "ty==0.0.79",
]
[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]   # line length is the formatter's job
